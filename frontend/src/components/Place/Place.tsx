import cn from "classnames";

import { PlaceProps } from "./types";
import classes from "./Places.module.css";

export function Place(props: PlaceProps) {
  const { id, lat, lon, category, onClick } = props;

  return null;
}
